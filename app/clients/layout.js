export const metadata = {
  title: 'Clients — Denzel Tinashe',
  description: 'Private client workspaces and project operations.',
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
      noimageindex: true,
    },
  },
};

export default function ClientsLayout({ children }) {
  return children;
}
